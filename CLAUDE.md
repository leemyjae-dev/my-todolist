# CLAUDE.md

LLM 코딩 시 흔한 실수를 줄이기 위한 행동 지침. 프로젝트별 지침과 함께 적용한다.

**트레이드오프**: 이 지침은 속도보다 신중함에 무게를 둔다. 사소한 작업에는 판단력을 발휘해 유연하게 적용한다.

## 1. 코딩 전에 생각하기

**추측하지 않는다. 혼란을 숨기지 않는다. 트레이드오프를 드러낸다.**

구현하기 전에:
- 가정을 명시적으로 밝힌다. 불확실하면 질문한다.
- 여러 해석이 가능하면 제시한다 — 임의로 하나만 골라 조용히 진행하지 않는다.
- 더 단순한 방법이 있으면 그렇게 말한다. 타당하면 반박한다.
- 불명확한 부분이 있으면 멈춘다. 무엇이 헷갈리는지 짚고 질문한다.

## 2. 단순함 우선

**문제를 해결하는 최소한의 코드. 추측성 코드는 없다.**

- 요청받지 않은 기능을 추가하지 않는다.
- 한 번만 쓰이는 코드에 추상화를 만들지 않는다.
- 요청하지 않은 "유연성"이나 "설정 가능성"을 넣지 않는다.
- 일어날 수 없는 상황에 대한 에러 처리를 넣지 않는다.
- 200줄을 썼는데 50줄로 줄일 수 있다면 다시 쓴다.

스스로에게 물어본다: "시니어 엔지니어가 이걸 보면 과하다고 할까?" 그렇다면 단순화한다.

## 3. 외과적 수정

**꼭 필요한 부분만 건드린다. 자신이 만든 흔적만 정리한다.**

기존 코드를 수정할 때:
- 인접한 코드/주석/포맷을 "개선"하지 않는다.
- 망가지지 않은 것을 리팩터링하지 않는다.
- 자신의 스타일이 다르더라도 기존 스타일을 따른다.
- 관련 없는 죽은 코드를 발견하면 언급만 하고 삭제하지 않는다.

변경 사항이 고아 코드를 만들었다면:
- 자신의 변경으로 인해 더 이상 쓰이지 않게 된 import/변수/함수는 제거한다.
- 요청받지 않은 이상 원래 있던 죽은 코드는 제거하지 않는다.

기준: 변경된 모든 줄은 사용자의 요청으로 직접 추적 가능해야 한다.

## 4. 목표 지향 실행

**성공 기준을 정의한다. 검증될 때까지 반복한다.**

작업을 검증 가능한 목표로 바꾼다:
- "검증 추가" → "잘못된 입력에 대한 테스트를 작성하고, 통과하도록 만든다"
- "버그 수정" → "버그를 재현하는 테스트를 작성하고, 통과하도록 만든다"
- "X 리팩터링" → "리팩터링 전후로 테스트가 통과하는지 확인한다"

여러 단계로 이루어진 작업은 간단한 계획을 먼저 제시한다:
```
1. [단계] → 검증: [확인 방법]
2. [단계] → 검증: [확인 방법]
3. [단계] → 검증: [확인 방법]
```

강한 성공 기준은 독립적으로 반복 작업할 수 있게 해준다. 약한 기준("동작하게 만들어라")은 계속된 확인 질문을 필요로 한다.

## 참조 문서

my-todoList 프로젝트 작업 시 아래 문서를 우선 참고한다.

| 문서명 | 경로 | 요약 |
|---|---|---|
| 도메인 정의서 | `docs/1-domain-definition.md` | User/Category/Todo 엔티티, 할일 상태(NOT_STARTED/IN_PROGRESS/OVERDUE/COMPLETED) 전이 규칙, 핵심 비즈니스 규칙(BR-01~09), 유스케이스(UC-01~09) 정의 |
| PRD | `docs/2-PRD.md` | 목표 사용자, MVP 범위(Must/Should/Could), 기능/비기능 요구사항, 확정 기술스택(React 19+TS+Zustand+TanStack Query, Node.js+Express+pg, PostgreSQL 17, JWT), 1인 개발/2일 일정, 1,000명 동시접속 목표 |
| 사용자 시나리오 | `docs/3-user-scenario.md` | UC-01~09를 사용자 관점 단계별 흐름(기본/예외 흐름, 사전·사후조건)으로 구체화 |
| 와이어프레임 | `docs/4-wireframe.md` | MVP 화면(S1~S8) ASCII 레이아웃, 데스크톱/모바일 반응형 차이, 화면 간 네비게이션 플로우 |
| 프로젝트 구조 설계 원칙 | `docs/5-project-principle.md` | 공통/레이어/네이밍/테스트/보안 원칙, 프론트엔드 FSD(app/pages/widgets/features/entities/shared) 디렉토리 구조, 백엔드 Route-Controller-Service-Query 구조 |
| 아키텍처 다이어그램 | `docs/6-arch.md` | 전체 시스템 구성도, 백엔드 요청 처리 흐름, 프론트엔드 FSD 레이어 구성, JWT 인증 흐름(mermaid) |
| ERD | `docs/7-erd.md` | users/categories/todos 3개 테이블 관계, 컬럼/타입/제약, 인덱스 정의 |
| DB 스키마(DDL) | `docs/schema.sql` | ERD 기반 PostgreSQL 17 `CREATE TABLE` 문 (FK, UNIQUE, CHECK, 인덱스 포함) |
| 실행 계획(WBS) | `docs/8-plan.md` | DB/백엔드/프론트엔드 Task 단위 작업 계획, Task별 수행 작업·완료 조건(체크박스)·선행 Task |
| 스타일 가이드 | `docs/9-style.md` | 컬러 팔레트(할일 상태 4종 포함), 타이포그래피, 여백/그리드, 버튼·입력필드·카드·모달·배지 컴포넌트 스타일 |
| API 스펙 | `backend/swagger.json` | OpenAPI 3.1 스펙. auth/users/categories/todos/health 13개 엔드포인트, JWT bearerAuth, 요청/응답 스키마 및 BR/UC 추적 설명 포함 |

---

**이 지침이 잘 작동하고 있다는 신호**: diff에 불필요한 변경이 줄어들고, 과설계로 인한 재작업이 줄어들며, 확인 질문이 실수가 발생한 뒤가 아니라 구현 전에 이루어진다.

---
(출처: https://raw.githubusercontent.com/multica-ai/andrej-karpathy-skills/refs/heads/main/CLAUDE.md)
